<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FirstAndroidApp" parent="Theme.MaterialComponents.DayNight">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:colorControlNormal">@color/black</item>
        <item name="android:colorBackground">@color/purple</item>
        <item name="android:strokeColor">@color/white</item>
        <item name="android:colorPressedHighlight">@color/cyan</item>
        <item name="android:iconTint">@color/white</item>

    </style>
</resources>